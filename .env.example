# S1+ only. The P0 intake pass makes zero API calls by definition (spec section 7, rung S0).
# The meter and usd_cap come up before the first paid call (PS-8).
DEEPSEEK_API_KEY=

# Optional overrides (defaults come from scriptorium.lock):
# SCRIPTORIUM_OCR_URL=http://127.0.0.1:8091
